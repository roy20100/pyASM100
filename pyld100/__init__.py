"""pyld100 - a linker for pyasm100's .APO object files.

Reads .APO object modules, resolves external references, assigns
addresses, and emits program_space (PS) and main_data (MD) text files
in the format asm2lm.py expects.
"""
